#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

"$basedir/../../packages/@ax/axunit-runner-mc7plus-win-x64/bin/AxUnit.Runner.exe"   "$@"
exit $?
